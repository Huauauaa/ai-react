/**
 * boneyard-js registry placeholder.
 *
 * Running `pnpm bones:build` will generate real registry + `*.bones.json` files
 * into `src/bones/`. Keeping this file prevents dev builds from failing before
 * the first capture.
 */
export {}

